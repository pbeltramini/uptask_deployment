extends layout

include mixins/crearProyecto

block contenido
    .contenido-principal
        h1 #{nombrePagina}

        pre= vardump(proyecto)

        if errrores
            each error in errores 
                .alerta= error.texto
        +crearProyecto(proyecto)

        